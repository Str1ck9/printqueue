textual>=0.47.0
# Cloud auth + device list + MQTT telemetry + camera protocol (AGPL-3.0;
# pulls paho-mqtt, requests, and — for features we don't use — flask +
# opencv-python).
bambu-lab-cloud-api==1.0.5
# Camera rendering in the TUI. Uses Sixel / Kitty graphics for crisp frames
# on capable terminals (iTerm2, kitty, WezTerm), half-block Unicode elsewhere.
# Pulls in pillow.
textual-image>=0.13
